# Routing Structure Check

- Current routes unchanged: `/` (landing), `/early-access`, and catch-all `*` handled in `src/App.tsx`.
- New sections are on the landing page only; no additional routes required.
